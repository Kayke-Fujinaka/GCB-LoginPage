const colors = {
  primaryColor: "#423B32",
  secondColor: "rgba(255, 255, 255, 0.56);", // Opacity 56%
  label: "#8B6B4A",
};

const fonts = {
  weight: {
    medium: 400,
    bold: 500,
    bolder: 600,
  },
  primary: "'Poppins', sans-serif",
};

const theme = {
  fonts,
  background: "#FFEBD6",
  ...colors,
};

export default theme;
